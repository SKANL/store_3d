import 'package:flutter/material.dart';

import '../templates/cart_template.dart';

class CartPage extends StatelessWidget {
  const CartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const CartTemplate();
  }
}